<%= form_for(@potential_answer) do |f| %>
  <% if @potential_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@potential_answer.errors.count, "error") %> prohibited this potential_answer from being saved:</h2>

      <ul>
      <% @potential_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :questions_id %><br>
    <%= f.text_field :questions_id %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </div>
  <div class="field">
    <%= f.label :answer_weight %><br>
    <%= f.number_field :answer_weight %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
